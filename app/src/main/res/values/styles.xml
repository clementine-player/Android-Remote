<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        The window behind the app's Compose screens, which draw themselves in ClementineTheme.
        Its background is ClementineTheme's surface, so nothing flashes before they draw; the
        night version is in values-night.
    -->
    <style name="Theme.Clementine" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

</resources>
