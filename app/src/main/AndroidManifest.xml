<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- Only needed to write downloads outside the app's own folder before Android 10. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The service stays in the foreground while connected to Clementine. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Apps the remote hands off to: a music player for downloads, a browser for links. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="audio/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:name="de.qspool.clementineremote.App"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:enableOnBackInvokedCallback="false"
        >

        <activity android:name=".ui.ConnectActivity"
                  android:exported="true"
                  android:configChanges="orientation|keyboardHidden|screenSize"
                  android:launchMode="singleTop"
                  android:theme="@style/AppTheme.PlayerTheme"
                  android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Back is handled in Compose, so it can be predictive (Android 14+ per activity). -->
        <activity android:name=".ui.MainActivity"
                  android:theme="@style/AppTheme.PlayerTheme"
                  android:enableOnBackInvokedCallback="true"
                  tools:targetApi="34"
                  android:exported="false"
                  android:launchMode="singleTop" />
        <activity android:name=".ui.settings.ClementineSettings"
                  android:exported="false"
                  android:theme="@style/AppTheme.PreferenceTheme"/>
        <activity android:name=".ui.TaskerSettings"
                  android:exported="true"
                  android:theme="@style/AppTheme.PreferenceTheme">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity>
        <service android:name=".backend.ClementineService"
                 android:exported="false"
                 android:foregroundServiceType="connectedDevice" />
        <receiver android:name=".backend.receivers.ClementinePhoneStateCheck"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>
        <receiver android:name=".backend.receivers.ClementineBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="de.qspool.clementineremote.playpause" />
                <action android:name="de.qspool.clementineremote.next" />
            </intent-filter>
        </receiver>
        <receiver android:name=".backend.receivers.TaskerFireReceiver"
                  android:exported="true">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
            </intent-filter>
        </receiver>

        <receiver android:name="de.qspool.clementineremote.widget.ClementineWidgetProvider"
                  android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                       android:resource="@xml/clementine_widget_provider"/>
        </receiver>

        <!-- Shares downloaded songs with music players on Android 9 and earlier; later
             versions save them to MediaStore. -->
        <provider android:name="androidx.core.content.FileProvider"
                  android:authorities="${applicationId}.downloads"
                  android:exported="false"
                  android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                       android:resource="@xml/download_paths" />
        </provider>
    </application>

</manifest>
