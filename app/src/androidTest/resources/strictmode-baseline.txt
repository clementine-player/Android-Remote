# StrictMode violations the app is known to have, one signature per line, each with the
# reason it is accepted for now. StrictModeDeviceTest fails on any signature not listed.
# Signature format: <ViolationType> at <first app class.method in the stack>

DiskReadViolation at de.qspool.clementineremote.ui.ConnectActivity.onCreate  # First read of the default SharedPreferences (small XML) at launch. Standard practice; removed if settings move to Jetpack DataStore.
